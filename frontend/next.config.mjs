/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,

  /*async rewrites() {
    return [
      {
        source: '/api/leads',
        destination: 'http://localhost:5001/api/leads'
      }
    ]
  },*/
};

export default nextConfig;
